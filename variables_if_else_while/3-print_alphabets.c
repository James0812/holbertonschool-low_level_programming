#include <stdio.h>

/**
 * main - print prints the alphabet in lowercase, and then in uppercase
 * Return: 0
 */
int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar (c);

	for (C = 'A'; c <= 'Z'; c++)
	putachar (c);
     putchar('/n');
    
     Return (0);
}     
